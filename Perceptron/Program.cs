using System;

namespace Perceptron
{
    class Program
    {
        static void Main(string[] args)
        {
            
        }

        //Funcion de activación
        public float funcion(float d)
        {
            return d > 0 ? 1 : 0;
        }
    }
}
